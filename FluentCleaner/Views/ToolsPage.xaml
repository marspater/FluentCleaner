<Page
    x:Class="FluentCleaner.Views.ToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ext="using:FluentCleaner.Tools"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="Transparent">

    <!-- Master-detail layout: left = extension switcher, right = active extension card + output -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" MinWidth="180" MaxWidth="340" />
            <ColumnDefinition Width="1" />     <!-- divider -->
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: extension switcher widget card -->
        <Border Grid.Column="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="{StaticResource FC_CardCornerRadius}"
                Margin="16,16,8,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />   <!-- widget header -->
                    <RowDefinition Height="Auto" />   <!-- divider -->
                    <RowDefinition Height="*" />       <!-- list -->
                </Grid.RowDefinitions>

                <!-- widget header: title + status left, category filter right -->
                <Grid Grid.Row="0" Margin="14,12,10,10">
                    <StackPanel VerticalAlignment="Center" Spacing="2">
                        <TextBlock Text="Extensions" Style="{StaticResource FC_CardHeaderStyle}" />
                        <TextBlock x:Name="lblStatus" Style="{StaticResource FC_CaptionStyle}" />
                    </StackPanel>
                    <ComboBox x:Name="comboFilter"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              FontSize="11"
                              SelectionChanged="comboFilter_SelectionChanged" />
                </Grid>

                <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" />

                <!-- extension list -->
                <ListView x:Name="listTools" Grid.Row="2"
                          SelectionMode="Single"
                          Padding="4,4,4,8"
                          SelectionChanged="listTools_SelectionChanged"
                          Visibility="Collapsed">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="ext:ToolsDefinition">
                            <StackPanel Orientation="Horizontal" Spacing="10" Padding="4,4">
                                <TextBlock Text="{x:Bind Icon}" FontSize="16"
                                           VerticalAlignment="Center" />
                                <TextBlock Text="{x:Bind Title}" Style="{StaticResource FC_BodyTextStyle}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Vertical Divider -->
        <Rectangle Grid.Column="1"
                   Width="1"
                   Fill="{ThemeResource DividerStrokeColorDefaultBrush}"
                   VerticalAlignment="Stretch" />

        <!-- RIGHT: active extension card + output -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />       <!-- widget card -->
                <RowDefinition Height="Auto" />    <!-- output panel -->
            </Grid.RowDefinitions>

            <!-- placeholder: no extensions folder -->
            <StackPanel x:Name="panelNoFolder" Grid.Row="0"
                        VerticalAlignment="Center" HorizontalAlignment="Center"
                        Spacing="10" Margin="24"
                        Visibility="Collapsed">
                <FontIcon Glyph="&#xE8F4;" FontSize="36"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                          HorizontalAlignment="Center" />
                <TextBlock Text="No extensions installed."
                           Style="{StaticResource FC_CardHeaderStyle}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Extensions are optional. Download the extension pack and place the Extensions folder next to the app."
                           Style="{StaticResource FC_SubtextStyle}"
                           TextWrapping="WrapWholeWords" TextAlignment="Center"
                           MaxWidth="340" />
                <HyperlinkButton Content="Get extensions on GitHub"
                                 Click="btnGitHub_Click"
                                 HorizontalAlignment="Center"
                                 FontSize="12" />
                <Button Content="Open app folder"
                        Click="btnOpenFolder_Click"
                        HorizontalAlignment="Center"
                        FontSize="12" />
            </StackPanel>

            <!-- placeholder: nothing selected yet -->
            <StackPanel x:Name="panelPlaceholder" Grid.Row="0"
                        VerticalAlignment="Center" HorizontalAlignment="Center"
                        Spacing="10">
                <FontIcon Glyph="&#xE7C5;" FontSize="36"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                          HorizontalAlignment="Center" />
                <TextBlock Text="Select an extension from the list."
                           Style="{StaticResource FC_CardHeaderStyle}"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <!-- widget card: active extension -->
            <ScrollViewer x:Name="scrollDetails" Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          Visibility="Collapsed">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="{StaticResource FC_CardCornerRadius}"
                        Margin="12,16,16,12" Padding="{StaticResource FC_CardPadding}">
                    <StackPanel Spacing="14">

                        <!-- widget header: icon + title left, folder button right -->
                        <Grid>
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <TextBlock x:Name="lblIcon" FontSize="18" VerticalAlignment="Center" />
                                <TextBlock x:Name="lblTitle" Style="{StaticResource FC_CardHeaderStyle}"
                                           VerticalAlignment="Center" TextWrapping="Wrap" />
                            </StackPanel>
                            <Button HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    ToolTipService.ToolTip="Open Extensions folder"
                                    AutomationProperties.Name="Open Extensions folder"
                                    Click="btnOpenFolder_Click"
                                    Style="{StaticResource FC_SubtleIconButton}">
                                <FontIcon Glyph="&#xE8A7;" FontSize="14" />
                            </Button>
                        </Grid>

                        <!-- description -->
                        <TextBlock x:Name="lblDescription" Style="{StaticResource FC_SubtextStyle}" TextWrapping="Wrap" />

                        <!-- help info bar -->
                        <InfoBar x:Name="infoHelp" Severity="Warning"
                                 IsOpen="False" IsClosable="False">
                            <InfoBar.ActionButton>
                                <Button x:Name="btnShowHelp" Content="Show help"
                                        Click="btnShowHelp_Click" FontSize="11" />
                            </InfoBar.ActionButton>
                        </InfoBar>

                        <!-- options dropdown -->
                        <ComboBox x:Name="comboOptions"
                                  HorizontalAlignment="Stretch"
                                  PlaceholderText="Select option"
                                  Visibility="Collapsed" />

                        <!-- free-text input -->
                        <TextBox x:Name="textInput"
                                 HorizontalAlignment="Stretch"
                                 PlaceholderText="Enter input (e.g. IDs or raw arguments)"
                                 Visibility="Collapsed" />

                        <!-- attribution link -->
                        <HyperlinkButton x:Name="linkPoweredBy"
                                         Visibility="Collapsed"
                                         FontSize="11" Padding="0"
                                         Click="linkPoweredBy_Click" />

                        <!-- action buttons -->
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="btnRun" Content="Run"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Click="btnRun_Click"
                                    Visibility="Collapsed" />
                            <Button x:Name="btnUninstall" Content="Remove"
                                    Click="btnUninstall_Click"
                                    Visibility="Collapsed" />
                        </StackPanel>

                        <ProgressRing x:Name="progressRing"
                                      IsActive="False"
                                      Width="24" Height="24"
                                      HorizontalAlignment="Left" />
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <!-- output widget card -->
            <Border x:Name="borderOutput" Grid.Row="1"
                    Margin="12,0,16,16">
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="{StaticResource FC_CardCornerRadius}"
                        Padding="{StaticResource FC_CardPadding}">
                    <StackPanel Spacing="8">
                        <Grid>
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <FontIcon Glyph="&#xE756;" FontSize="14"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="Output" Style="{StaticResource FC_CardHeaderStyle}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <!-- more-options flyout (...) -->
                            <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Style="{StaticResource FC_SubtleIconButton}"
                                    ToolTipService.ToolTip="More options"
                                    AutomationProperties.Name="More options">
                                <FontIcon Glyph="&#xE712;" FontSize="14" />
                                <Button.Flyout>
                                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                                        <MenuFlyoutItem x:Name="btnClearLog"
                                                        Text="Clear output"
                                                        Click="btnClearLog_Click">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE74D;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </Button.Flyout>
                            </Button>
                        </Grid>
                        <TextBox x:Name="txtLog"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 FontFamily="Cascadia Code, Consolas"
                                 FontSize="11"
                                 MaxHeight="120"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 PlaceholderText="Extension output will appear here..." />
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Grid>
</Page>
