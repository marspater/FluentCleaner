<Page
    x:Class="FluentCleaner.Views.DeveloperCleanupView"
    NavigationCacheMode="Required"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:FluentCleaner.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ProgressBar Grid.Row="0"
                     VerticalAlignment="Top"
                     IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,26,32,32" Spacing="22" HorizontalAlignment="Stretch">
                
                <TextBlock Text="Workspace Purge" Style="{StaticResource TitleTextBlockStyle}" />

                <StackPanel Spacing="10">
                    <TextBlock Text="Configuration" FontSize="13" FontWeight="SemiBold" />
                    
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="18,14">
                        <StackPanel Spacing="14">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <TextBox Grid.Column="0"
                                         PlaceholderText="Select project or workspace directory (e.g. C:\Projects)"
                                         Text="{x:Bind ViewModel.RootPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}"
                                         FontSize="12" />
                                         
                                <Button Grid.Column="1" Content="Browse…"
                                        Click="BrowseButton_Click"
                                        IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" />

                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                                    <Button Content="Analyze"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Command="{x:Bind ViewModel.ScanCommand}"
                                            MinWidth="90" />
                                    <Button Content="Cancel"
                                            Command="{x:Bind ViewModel.CancelCommand}"
                                            Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                            MinWidth="90" />
                                </StackPanel>
                            </Grid>

                            <StackPanel Orientation="Horizontal" Spacing="24" Margin="2,4,0,0">
                                <CheckBox Content="node_modules (JS/TS)"
                                          IsChecked="{x:Bind ViewModel.ScanNodeModules, Mode=TwoWay}"
                                          IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" />
                                <CheckBox Content="bin &amp; obj (.NET)"
                                          IsChecked="{x:Bind ViewModel.ScanBinObj, Mode=TwoWay}"
                                          IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" />
                                <CheckBox Content="target (Rust)"
                                          IsChecked="{x:Bind ViewModel.ScanTarget, Mode=TwoWay}"
                                          IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" />
                                <CheckBox Content="build &amp; dist (Web/Build)"
                                          IsChecked="{x:Bind ViewModel.ScanBuildDist, Mode=TwoWay}"
                                          IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="10" Visibility="{x:Bind ViewModel.HasResults, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Discovered Build Directories" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <CheckBox Grid.Column="1" Content="Select all"
                                  IsChecked="{x:Bind ViewModel.SelectAll, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.IsNotBusy, Mode=OneWay}"
                                  VerticalAlignment="Center" />
                    </Grid>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8">
                        <ListView ItemsSource="{x:Bind ViewModel.TrashDirectories, Mode=OneWay}"
                                  SelectionMode="None"
                                  Padding="0,4">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="MinHeight" Value="0" />
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vm:TrashDirectoryViewModel">
                                    <Grid Padding="18,10" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                                  VerticalAlignment="Center"
                                                  MinWidth="0"
                                                  Padding="8,0,0,0">
                                            <StackPanel Spacing="1" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind FolderName}" FontWeight="SemiBold" FontSize="12.5" />
                                                <TextBlock Text="{x:Bind ParentPath}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </CheckBox>

                                        <TextBlock Grid.Column="1"
                                                   Text="{x:Bind SizeText, Mode=OneWay}"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="12,0,4,0" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="18,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0"
                                   Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   TextWrapping="WrapWholeWords"
                                   Margin="0,0,12,0" />
                                   
                        <Button Grid.Column="1"
                                Content="Nuke Selected"
                                Style="{StaticResource AccentButtonStyle}"
                                Command="{x:Bind ViewModel.NukeCommand}"
                                MinWidth="120"
                                Height="32" />
                    </Grid>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
